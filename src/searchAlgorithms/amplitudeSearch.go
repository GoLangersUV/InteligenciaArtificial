package searchAlgorithms

import (
  "time"
)

type AmplitudeSearch struct {
  solutionPath [][]int
}

func (a *AmplitudeSearch) LookForGoal(e *enviroment) (solutionFound bool, expandenNodes, treeDepth, cost float32, timeExe time.Duration) {
  // Generamos una percepción del entorno para ir expandiendo nodos

  // Se modifica a.solutionPath que es el camino a seguir para llegar al objetivo
  return
}

